# The chart api version.
apiVersion: v2
# The name of this chart.
name: kompass-storage
# The description of this chart.
description: A helm chart for Kubernetes Zesty Disk. A dynamic block storage auto-scaler in order to right-size EBS volumes, improve application stability, and significantly reduce storage costs while relieving engineers of the burden of managing volumes.
# The type of this chart.
type: application
# The version of this chart (follows Semantic versioning).
version: 1.7.2
# The application version.
appVersion: 1.7.1
